<h1>Listing Orders</h1>

<table class="table">
  <tr>
    <th>Name</th>
    <th>Id</th>
    <th>Location</th>
    <th>Contents</th>
    <th>Type</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @orders.each do |order| %>
  <tr>
    <td><%= order.name %></td>
    <td><%= order.id %></td>
    <td><%= order.location %></td>
    <td><%= order.contents %></td>
    <td><%= order.type %></td>
    <td><%= order.status %></td>
    <td><%= link_to 'Show', order %></td>
    <td><%= link_to 'Edit', edit_order_path(order) %></td>
    <td><%= link_to 'Destroy', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Order', new_order_path, class: "btn btn-mini" %> <%= link_to 'View Users', users_path, class: "btn btn-mini" %> <%= link_to 'Return Home', root_path, class: "btn btn-mini" %>
